{
  "name": "guestflow-healthcare",
  "private": true,
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "scripts": {
    "dev": "turbo dev",
    "build": "turbo build",
    "lint": "turbo lint",
    "dev:api": "turbo dev --filter=@guestflow/api",
    "dev:paramedic": "turbo dev --filter=@guestflow/paramedic",
    "dev:doctor": "turbo dev --filter=@guestflow/doctor"
  },
  "devDependencies": {
    "turbo": "^2.9.14"
  }
}
